function Contact(props) {
    return (
        <div>
            <div>Contact Page of {props.name}</div>
            <input />
            <button>Submit</button>
        </div>
       
    )
}

export default Contact